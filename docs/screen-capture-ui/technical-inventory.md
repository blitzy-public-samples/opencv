# 1. Capture-Relevant APIs

| API or module name | Purpose | Pipeline stage | Maturity / status in this build | External dependency required |
| --- | --- | --- | --- | --- |
| Survey revision — branch `5.x`, commit `0627765f01be7ea464846ea1e56bbf4e6d861bcf` | The single revision of this repository at which every source citation in this file was read; a line locator is checkable only against this revision. | Not Found | Survey artefact rather than an API: this row records provenance, not a capability. No build option declares or gates it. No configure step was run against this checkout, so the configured availability of everything inventoried below is unknown here. | no |
| `cv::VideoCapture`, five `open` overloads [modules/videoio/include/opencv2/videoio.hpp:864,877,888,901,914] | Opens a capture source and reads frames from it. The five overloads present three open shapes: by device index, by filename, and by a stream source with a typed integer parameter vector. The filename parameter is documented to accept a video file, an image sequence, a stream URL, or a GStreamer pipeline string when GStreamer is the backend [modules/videoio/include/opencv2/videoio.hpp:799-805]. | Frame acquisition | Public installed API, declared unconditionally in the installed header [modules/videoio/include/opencv2/videoio.hpp:864]. Which backend services a given open is gated by the per-backend options in section 5 and by the declared capability modes of each registry entry [modules/videoio/src/videoio_registry.hpp:15-20]. No configure step was run against this checkout, so configured availability is unknown here. | no |
| `cv::VideoCapture::release` [modules/videoio/include/opencv2/videoio.hpp:930] | Closes the capture and releases the resources it holds, which is the teardown half of a capture session's lifecycle. | Frame acquisition | Public installed API, declared unconditionally in the installed header [modules/videoio/include/opencv2/videoio.hpp:930]. No build option gates the declaration; the behaviour of the release is the active backend's. No configure step was run against this checkout, so configured availability of any backend is unknown here. | no |
| `cv::VideoWriter`, with `open`, `release` and `fourcc` [modules/videoio/include/opencv2/videoio.hpp:1076,1150,1177,1230] | Encodes and writes frames to a file or stream. Relevant to a capture workload as the route for retaining a session as video rather than as discrete images. | Persistence | Public installed API, declared unconditionally in the installed header [modules/videoio/include/opencv2/videoio.hpp:1076]. Writer availability per backend is gated by the `MODE_WRITER` capability declared on each registry entry [modules/videoio/src/videoio_registry.hpp:15-20] and by the options in section 5. No configure step was run against this checkout, so configured availability is unknown here. | no |
| `cv::VideoCapture::waitAny` [modules/videoio/include/opencv2/videoio.hpp:1035-1053] | The only frame-readiness notification API on the capture surface: it waits for any of several open captures to have a frame ready, and is documented for multi-camera environments. | Frame acquisition | Public installed API, but functional on one backend only. The implementation asserts that all captures share a backend, dispatches to V4L, and otherwise raises `StsNotImplemented` with the message that `waitAny` is supported by the V4L backend only [modules/videoio/src/cap.cpp:629-652]. It is therefore gated by `WITH_V4L` [CMakeLists.txt:320-322] and by a Linux Video4Linux interface. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `cv::videoio_registry` list queries — `getBackends`, `getCameraBackends`, `getStreamBackends`, `getStreamBufferedBackends`, `getWriterBackends` [modules/videoio/include/opencv2/videoio/registry.hpp:30,33,36,39,42] | The five public queries by which an application discovers which backends are available and in which role. They partition backends by all-available, device-index capture, filename capture, buffered-stream capture and writer. None of the five is screen-oriented, and the presence of an all-backends query means no screen-oriented backend is being hidden by the partitioning. | Source discovery | Public installed API, declared unconditionally in the installed header [modules/videoio/include/opencv2/videoio/registry.hpp:30]. What each returns is gated by which backends the build actually enables, per section 5. No configure step was run against this checkout, so configured availability is unknown here. | no |
| `enum VideoCaptureAPIs` [modules/videoio/include/opencv2/videoio.hpp:91-122] | The complete set of backend identifiers an application may request. The enumeration declares 30 enumerators, of which six are aliases of an earlier value — `CAP_V4L2`, `CAP_FIREWARE`, `CAP_IEEE1394`, `CAP_DC1394`, `CAP_CMU1394` and `CAP_REALSENSE` — leaving 24 distinct numeric values, one of which, `CAP_ANY = 0`, is documented as auto-detection rather than a backend. That leaves 23 concrete backend identifiers, and every one of them names a camera family, a media framework, an image-sequence reader or a capture SDK. None targets screen, display, desktop, window or monitor content. | Source selection | Public installed API, declared unconditionally in the installed header [modules/videoio/include/opencv2/videoio.hpp:91-122]. Whether a given identifier resolves to anything is gated by the corresponding option in section 5 and by the registry entry's own build guard [modules/videoio/src/videoio_registry.cpp:66-193]. No configure step was run against this checkout, so configured availability is unknown here. | no |
| `CAP_GSTREAMER` manual-pipeline route [modules/videoio/include/opencv2/videoio.hpp:799-805] [modules/videoio/src/cap_gstreamer.cpp:1409-1438] | The stronger of the two routes by which an externally produced frame source, including one that acquires screen content, can reach `VideoCapture` without any screen backend existing. When the filename argument is neither a valid URI nor an existing file, the backend passes it to `gst_parse_launch` and marks the capture a manual pipeline [modules/videoio/src/cap_gstreamer.cpp:1409-1438]. The route is part of the documented parameter contract rather than incidental [modules/videoio/include/opencv2/videoio.hpp:799-805]. | Frame acquisition | Public installed API and a documented parameter contract, but conditional, and the condition is decisive: the pipeline must terminate in an appsink named either `appsink0`, the default, or `opencvsink` [modules/videoio/src/cap_gstreamer.cpp:1343]; the manual-pipeline branch searches the pipeline's elements for such a name [modules/videoio/src/cap_gstreamer.cpp:1502] and fails with a warning that it cannot find an appsink in the manual pipeline when there is none [modules/videoio/src/cap_gstreamer.cpp:1534]. For live sources the header comment directs the caller to add the appsink drop parameter [modules/videoio/src/cap_gstreamer.cpp:1346]. The route is not discoverable through any registry query and does not make the screen a first-class source. Gated by `WITH_GSTREAMER`, declared ON [CMakeLists.txt:222-224]. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `CAP_FFMPEG` input-format route, mediated by `OPENCV_FFMPEG_CAPTURE_OPTIONS` [modules/videoio/src/cap_ffmpeg_impl.hpp:1184] | The weaker of the two routes by which an externally selected frame source can reach `VideoCapture`. The environment variable's value is parsed into an option dictionary, with a semicolon separating a key from its value and a vertical bar separating one pair from the next [modules/videoio/src/cap_ffmpeg_impl.hpp:1197]; an `input_format` key in that dictionary is resolved through `av_find_input_format` [modules/videoio/src/cap_ffmpeg_impl.hpp:1206-1210], which is what allows a demuxer to be named from outside the library. | Frame acquisition | Conditional, and weaker than the GStreamer route on two counts: it is mediated by an environment variable rather than a typed parameter, and it reaches only what `av_find_input_format` can resolve, which is a demuxer and not an arbitrary component. Device-oriented opening is additionally build-conditional on libavdevice [modules/videoio/src/cap_ffmpeg_impl.hpp:1213]; without it the backend logs that OpenCV should be configured with libavdevice to open a camera device and returns false [modules/videoio/src/cap_ffmpeg_impl.hpp:1246]. Not discoverable through any registry query. Gated by `WITH_FFMPEG`, whose declared default is the conditional expression `(NOT ANDROID)` [CMakeLists.txt:219-221]. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `CAP_PROP_OPEN_TIMEOUT_MSEC = 53` and `CAP_PROP_READ_TIMEOUT_MSEC = 54` [modules/videoio/include/opencv2/videoio.hpp:187-188] | The two timeouts bounding how long an open and a read may stall. Both are documented open-only and applicable to the FFmpeg and GStreamer back-ends only, so they are the only stall bounds available on the two ingestion routes above. | Frame acquisition | Public installed property identifiers; those two lines define the identifiers and their applicability, not their values. Each of the two backends independently defines a 30-second default in its own implementation: FFmpeg at [modules/videoio/src/cap_ffmpeg_impl.hpp:261-262], assigned at [modules/videoio/src/cap_ffmpeg_impl.hpp:665-666]; GStreamer at [modules/videoio/src/cap_gstreamer.cpp:83-84], initialised at [modules/videoio/src/cap_gstreamer.cpp:422-423]. Gated by `WITH_FFMPEG` [CMakeLists.txt:219-221] and `WITH_GSTREAMER` [CMakeLists.txt:222-224]. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `CAP_PROP_PTS = 71` [modules/videoio/include/opencv2/videoio.hpp:205] | The presentation timestamp of the most recently read frame, expressed in the FPS time base. This is genuine per-frame metadata and is the closest the capture surface comes to a per-frame time value, but it is a media presentation timestamp and not a host-clock acquisition instant. | Timing metadata | Public installed property identifier, documented read-only and FFmpeg back-end only [modules/videoio/include/opencv2/videoio.hpp:205], so it is gated by `WITH_FFMPEG`, declared as `(NOT ANDROID)` [CMakeLists.txt:219-221], and is unavailable on every other backend. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `CAP_PROP_POS_MSEC = 0` [modules/videoio/include/opencv2/videoio.hpp:133] | The current position of the video file in milliseconds: an offset within the media timeline. It resembles a frame timestamp and is not one, since it locates a position in the stream rather than an instant at which a frame was acquired. | Timing metadata | Public installed property identifier, declared unconditionally in the installed header [modules/videoio/include/opencv2/videoio.hpp:133]; whether a given backend answers a get for it is the backend's own behaviour, gated by the options in section 5. No configure step was run against this checkout, so configured availability is unknown here. | no |
| `CAP_PROP_STREAM_OPEN_TIME_USEC = 55` [modules/videoio/include/opencv2/videoio.hpp:189] | The time in microseconds since 1 January 1970 at which the stream was opened. This is a civil-time anchor for the session, not for the frame: it is written once at open and says nothing about when any individual frame arrived. | Timing metadata | Public installed property identifier, documented read-only and applicable for the FFmpeg backend only [modules/videoio/include/opencv2/videoio.hpp:189], so it is gated by `WITH_FFMPEG`, declared as `(NOT ANDROID)` [CMakeLists.txt:219-221]. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `VIDEOWRITER_PROP_PTS = 12` [modules/videoio/include/opencv2/videoio.hpp:230] | Presentation metadata supplied to the writer, documented as necessary only when encapsulating externally encoded video whose decoding order differs from its presentation order. It is encoder-side input and carries no acquisition time, so it is not a capture timestamp and is not derived from any capture property. | Persistence | Public installed writer-property identifier, documented FFmpeg back-end only [modules/videoio/include/opencv2/videoio.hpp:230], so it is gated by `WITH_FFMPEG`, declared as `(NOT ANDROID)` [CMakeLists.txt:219-221]. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `CAP_PROP_FRAME_WIDTH = 3`, `CAP_PROP_FRAME_HEIGHT = 4`, `CAP_PROP_FPS = 5`, `CAP_PROP_BUFFERSIZE = 38` [modules/videoio/include/opencv2/videoio.hpp:136,137,138,172] | The property knobs against which any resolution and frame-rate tradeoff must be expressed, plus the internal buffer depth. They are the whole of the configuration surface the capture API offers for that tradeoff. | Frame acquisition | Public installed property identifiers, declared unconditionally in the capture-property enumeration [modules/videoio/include/opencv2/videoio.hpp:131-211]; whether a get or set is honoured is the active backend's behaviour and is gated by the options in section 5. No numeric rate or resolution figure is committed anywhere in this repository. No configure step was run against this checkout, so configured availability is unknown here. | no |
| Screen, display, desktop, window or monitor capture backend | A backend that would acquire the contents of a display as a first-class capture source, selectable by a `CAP_*` identifier and discoverable through the registry. Not Found. Obtaining screen pixels therefore depends on a host-side acquisition mechanism fed through one of the two ingestion routes above, which are not registry-discoverable and do not make the screen a first-class source. | Frame acquisition | Not Found, established by exhaustive enumeration of the three structures that would have to contain such a backend, not by a keyword search: of the 23 concrete identifiers in `enum VideoCaptureAPIs` none names a display surface [modules/videoio/include/opencv2/videoio.hpp:91-122]; every entry in the built-in backend table is tagged only with capture-by-index, capture-by-filename, capture-by-stream or writer capability [modules/videoio/src/videoio_registry.cpp:66-193]; and the capability mask defines bits 0, 1, 2 and 4, leaving bit 3 unused and reserving no screen-oriented mode [modules/videoio/src/videoio_registry.hpp:15-20]. The five public queries offer no screen-oriented query either [modules/videoio/include/opencv2/videoio/registry.hpp:30,33,36,39,42]. No build option would gate such a backend, since none is declared. | yes |

# 2. Processing-Relevant APIs

| API or module name | Purpose | Pipeline stage | Maturity / status in this build | External dependency required |
| --- | --- | --- | --- | --- |
| `accumulate`, `accumulateSquare`, `accumulateProduct`, `accumulateWeighted` [modules/imgproc/include/opencv2/imgproc.hpp:2683,2702,2721,2742] | The accumulation entry points of the motion analysis and object tracking group [modules/imgproc/include/opencv2/imgproc.hpp:189]. They maintain a running image, including a weighted running average, which is the reference-frame machinery a change comparison can be built against. | Change detection | Public installed API, declared unconditionally within the motion group block [modules/imgproc/include/opencv2/imgproc.hpp:2663-2819]. No build option gates the declaration; the implementation behind it may be substituted through the acceleration interface in this section. No configure step was run against this checkout, so the configured build was not verified here. | no |
| `phaseCorrelate`, `phaseCorrelateIterative`, `createHanningWindow` [modules/imgproc/include/opencv2/imgproc.hpp:2783,2800,2817] | The phase-correlation entry points of the same motion group. They estimate the translational shift between two images, with the Hanning window supplied as the standard pre-multiplication. Relevant to a screen workload for detecting that a region scrolled or moved rather than changed content. | Change detection | Public installed API, declared unconditionally within the motion group block [modules/imgproc/include/opencv2/imgproc.hpp:2663-2819]. No build option gates the declaration. No configure step was run against this checkout, so the configured build was not verified here. | no |
| `cvtColor` [modules/imgproc/include/opencv2/imgproc.hpp:3526] | Converts between colour representations. Load-bearing for a screen workload because a screen-shaped source buffer arrives with an alpha channel and a change comparison is normally run on a single-channel image. | Frame preprocessing | Public installed API, declared unconditionally in the installed header [modules/imgproc/include/opencv2/imgproc.hpp:3526]. No build option gates the declaration; the implementation may be substituted through the acceleration interface in this section. No configure step was run against this checkout, so the configured build was not verified here. | no |
| `GaussianBlur` [modules/imgproc/include/opencv2/imgproc.hpp:1276] | Low-pass filtering, applied before a change comparison so that compression noise and sub-pixel rendering differences do not register as change. | Frame preprocessing | Public installed API, declared unconditionally in the installed header [modules/imgproc/include/opencv2/imgproc.hpp:1276]. No build option gates the declaration. No configure step was run against this checkout, so the configured build was not verified here. | no |
| `resize` [modules/imgproc/include/opencv2/imgproc.hpp:2096] | Rescales a frame. Relevant as the only in-scope means of trading comparison cost against spatial precision when a change comparison is run at reduced scale. | Frame preprocessing | Public installed API, declared unconditionally in the installed header [modules/imgproc/include/opencv2/imgproc.hpp:2096]. No build option gates the declaration. No configure step was run against this checkout, so the configured build was not verified here. | no |
| `threshold`, `thresholdWithMask` [modules/imgproc/include/opencv2/imgproc.hpp:2849,2869] | Binarises a single-channel image, with the masked variant restricting the operation to a region. These interpret a difference image once one exists; they do not produce one. | Change interpretation | Public installed API, declared unconditionally in the installed header [modules/imgproc/include/opencv2/imgproc.hpp:2849]. No build option gates the declaration. No configure step was run against this checkout, so the configured build was not verified here. | no |
| `morphologyEx` [modules/imgproc/include/opencv2/imgproc.hpp:2051] | Compound morphological operations, used to close gaps in and remove speckle from a binarised change mask before regions are extracted from it. | Change interpretation | Public installed API, declared unconditionally in the installed header [modules/imgproc/include/opencv2/imgproc.hpp:2051]. No build option gates the declaration. No configure step was run against this checkout, so the configured build was not verified here. | no |
| `connectedComponents`, `connectedComponentsWithStats` [modules/imgproc/include/opencv2/imgproc.hpp:3704,3746] | Labels connected regions of a binary image, the statistics variant additionally yielding each region's bounding box, area and centroid. This is the primitive that turns a change mask into addressable changed regions. | Change interpretation | Public installed API, declared unconditionally in the installed header [modules/imgproc/include/opencv2/imgproc.hpp:3704]. No build option gates the declaration. No configure step was run against this checkout, so the configured build was not verified here. | no |
| `findContours` [modules/imgproc/include/opencv2/imgproc.hpp:3782] | Extracts region outlines from a binary image, giving changed-region geometry in a form that can be recorded or drawn. | Change interpretation | Public installed API, declared unconditionally in the installed header [modules/imgproc/include/opencv2/imgproc.hpp:3782]. No build option gates the declaration. No configure step was run against this checkout, so the configured build was not verified here. | no |
| `Canny` [modules/imgproc/include/opencv2/imgproc.hpp:1639,1657] | Edge detection, in a direct-image overload and a precomputed-gradient overload. Relevant as an interpretation aid for structure within a changed region rather than as a change detector. | Change interpretation | Public installed API, declared unconditionally in the installed header [modules/imgproc/include/opencv2/imgproc.hpp:1639]. No build option gates the declaration. No configure step was run against this checkout, so the configured build was not verified here. | no |
| `matchTemplate` [modules/imgproc/include/opencv2/imgproc.hpp:3663] | Locates a known template within a frame. Relevant where a screen element to be tracked is known in advance, which is a narrower problem than general change detection and does not substitute for it. | Change interpretation | Public installed API, declared unconditionally in the installed header [modules/imgproc/include/opencv2/imgproc.hpp:3663]. No build option gates the declaration. No configure step was run against this checkout, so the configured build was not verified here. | no |
| `line`, `rectangle`, `circle`, `polylines` [modules/imgproc/include/opencv2/imgproc.hpp:3888,3941,3970,4104] | The drawing primitives of the drawing functions group [modules/imgproc/include/opencv2/imgproc.hpp:130,3866], with a second `rectangle` overload taking a rectangle rather than two corners [modules/imgproc/include/opencv2/imgproc.hpp:3950]. These compose visual annotation into the image that is then displayed. | Annotation composition | Public installed API, declared unconditionally in the installed header [modules/imgproc/include/opencv2/imgproc.hpp:3888]. They draw into an existing image and hold no annotation state of their own. No build option gates the declaration. No configure step was run against this checkout, so the configured build was not verified here. | no |
| `putText`, `getTextSize` [modules/imgproc/include/opencv2/imgproc.hpp:4233,4283] | Renders a text string into an image, with the measurement call supplying the rendered extent so text can be laid out and boxed before it is drawn. | Annotation composition | Public installed API, declared unconditionally in the installed header [modules/imgproc/include/opencv2/imgproc.hpp:4233]. This is rendering of text into an image, which is unrelated to recognising text from an image; the latter is inventoried in section 4. No build option gates the declaration. No configure step was run against this checkout, so the configured build was not verified here. | no |
| Image-processing hardware acceleration interface [modules/imgproc/include/opencv2/imgproc.hpp:193-196] | The interface by which an implementation of an image-processing operation may be replaced by an accelerated one, declaring 77 distinct replaceable entry points [modules/imgproc/src/hal_replacement.hpp]. It is a compute-acceleration surface only: it is the extension point for how an operation is computed, not an extension point for acquiring frames or for displaying them. | Frame preprocessing | Private, non-installed interface: the entry points are declared in a module-internal header [modules/imgproc/src/hal_replacement.hpp] and are not part of the application-facing API. Nothing here bears on capture or display, and a complete enumeration of the videoio and highgui installed include trees finds no comparable interface for either. No configure step was run against this checkout, so which implementations are selected is unknown here. | no |
| Frame-to-frame change gate | A single call that admits or rejects a frame by how much it differs from its predecessor, yielding a change score. Not Found. The composition such a gate requires is also incomplete here: element-wise absolute difference, element-wise comparison and non-zero counting are not provided by the in-scope modules, and this survey does not identify what provides them. A change gate is therefore application code, assembled from the accumulation, thresholding, morphology and region-extraction primitives above, and its score definition must be specified by the application rather than inherited from a library. | Change detection | Not Found. Established by enumeration of the motion analysis and object tracking group, which declares seven entry points, all accumulation or phase correlation, and no differencing or admission operation [modules/imgproc/include/opencv2/imgproc.hpp:2663-2819], and by the absence of any such operation among the interpretation primitives inventoried above. No build option gates a capability that is not declared. | no |
| Statistical background subtraction | A model of a scene's background maintained over time, against which foreground change is segmented. Not Found: background subtraction is not provided by the in-scope modules, and this survey does not identify what provides it. | Change detection | Not Found. Established by enumeration of the motion analysis and object tracking group, whose seven declared entry points are accumulation and phase correlation only [modules/imgproc/include/opencv2/imgproc.hpp:2663-2819]; no background model class or factory is declared anywhere in the in-scope installed headers. No build option gates a capability that is not declared. | yes |
| Optical flow | Per-pixel or sparse motion estimation between consecutive frames. Not Found: optical flow is not provided by the in-scope modules, and this survey does not identify what provides it. The nearest in-scope capability is phase correlation, which estimates a single global translational shift between two images and is not a flow field. | Change detection | Not Found. Established by enumeration of the motion analysis and object tracking group, whose seven declared entry points are accumulation and phase correlation only [modules/imgproc/include/opencv2/imgproc.hpp:2663-2819]. No build option gates a capability that is not declared. | yes |


# 3. Display-Relevant APIs

| API or module name | Purpose | Pipeline stage | Maturity / status in this build | External dependency required |
| --- | --- | --- | --- | --- |
| `namedWindow`, `destroyWindow` [modules/highgui/include/opencv2/highgui.hpp:239,247] | Creates and destroys a named display window, which is the surface a preview or review pane would occupy and the object every other call in this section addresses by name. | Preview and review | Public installed API, declared unconditionally in the installed header [modules/highgui/include/opencv2/highgui.hpp:239]. Requires an active windowing backend, each of which is gated on a toolkit or platform facility detected at configure time, per the display options in section 5. The module's own account of itself frames it as a facility for trying functionality out quickly and visualising results [modules/highgui/include/opencv2/highgui.hpp:55-66], which bounds what it claims to be. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `imshow` [modules/highgui/include/opencv2/highgui.hpp:345] | Displays an image in a named window. This is the presentation call for a live preview, and video-rate display is explicitly within its documented use: the header states that a `waitKey(25)` call will display a frame and wait approximately 25 ms for a key press, and describes that as suitable for displaying a video frame-by-frame [modules/highgui/include/opencv2/highgui.hpp:332-333]. | Preview and review | Public installed API, declared unconditionally in the installed header [modules/highgui/include/opencv2/highgui.hpp:345]. Conditional on an active windowing backend and on the event pump below being driven. No numeric frame-rate figure is committed anywhere in this repository; the documented example above is an interval in a usage note, not a performance claim. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `waitKey`, `waitKeyEx`, `pollKey` [modules/highgui/include/opencv2/highgui.hpp:291,271,305] | The event pump and the keyboard input path in one: a blocking wait with a timeout, its full-key-code variant, and a non-blocking poll. The header states that these are the only methods in HighGUI that can fetch and handle GUI events, that one of them needs to be called periodically for normal event processing unless the surrounding environment handles it, and that they work only when at least one window has been created and is active [modules/highgui/include/opencv2/highgui.hpp:282-287]. | Preview and review | Public installed API, declared unconditionally in the installed header [modules/highgui/include/opencv2/highgui.hpp:291]. The periodic-call requirement and the active-window requirement are the portable contract and are not optional. Nothing in the installed header establishes a cross-platform rule about which thread owns a window or runs a callback. Conditional on an active windowing backend, gated per the display options in section 5. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `currentUIFramework` [modules/highgui/include/opencv2/highgui.hpp:261] | Returns the name of the active user-interface backend. This is the call that makes every backend-conditional verdict in this section resolvable at run time rather than guessed: the implementation returns the active backend's registered name where one is present and otherwise falls through to a compile-time built-in [modules/highgui/src/window.cpp:1096-1118]. | Backend discovery | Public installed API, declared unconditionally in the installed header [modules/highgui/include/opencv2/highgui.hpp:261], and callable without any backend being present, which is what makes it usable as a probe before an application commits to a backend-conditional feature. No build option gates the declaration. No configure step was run against this checkout, so which name it would return here is unknown. | no |
| `startWindowThread` [modules/highgui/include/opencv2/highgui.hpp:264] | Documented as starting a thread that handles window events, which reads as a way to avoid driving the event pump by hand. | Preview and review | Public installed API, declared unconditionally in the installed header [modules/highgui/include/opencv2/highgui.hpp:264], but its effect exists on one backend only: the GTK implementation starts an event thread [modules/highgui/src/window_gtk.cpp:647-664], while the non-GTK definition returns 0 with no effect [modules/highgui/src/window.cpp:902-908]. It is therefore a backend-specific facility gated by `WITH_GTK`, declared ON [CMakeLists.txt:225-227], and not a portable substitute for calling the pump periodically. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `setMouseCallback` [modules/highgui/include/opencv2/highgui.hpp:427] | Registers a pointer-event callback on a named window, which is the whole of the pointer input available for placing or selecting an annotation. | Interaction | Public installed API, declared unconditionally in the installed header [modules/highgui/include/opencv2/highgui.hpp:427], and conditional on the active backend: the framebuffer backend accepts the registration and does nothing with it, logging that it is not supported [modules/highgui/src/window_framebuffer.cpp:322-324]. A public declaration therefore does not imply uniform backend support, and an application depending on pointer input must determine the active backend at run time through `currentUIFramework` and degrade explicitly. Gated per the display options in section 5. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `selectROI` [modules/highgui/include/opencv2/highgui.hpp:463,467] | Lets the user drag out a rectangle over a displayed image and returns it, in a named-window overload and one that manages its own window. This is the ready-made region-selection interaction for annotating a captured frame. | Interaction | Public installed API, declared unconditionally in the installed header [modules/highgui/include/opencv2/highgui.hpp:463]. It is built on pointer input and the event pump, so it inherits their backend conditions, including the framebuffer backend's non-implementation of pointer callbacks [modules/highgui/src/window_framebuffer.cpp:322-324]. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `createTrackbar`, `getTrackbarPos`, `setTrackbarPos`, `setTrackbarMax`, `setTrackbarMin` [modules/highgui/include/opencv2/highgui.hpp:517,532,545,558,571] | The five trackbar entry points: creation, position read and write, and range bounds. Together they are the only in-scope control surface for a numeric parameter, and the bounds setters are what would let a slider address a frame index rather than a fixed range. | Interaction | Public installed API, declared unconditionally in the installed header [modules/highgui/include/opencv2/highgui.hpp:517], and conditional on the active backend: the framebuffer backend implements neither creation nor lookup, logging in each case that it is not supported [modules/highgui/src/window_framebuffer.cpp:327-333,339]. Gated per the display options in section 5. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `createButton` [modules/highgui/include/opencv2/highgui.hpp:808-810] | Adds a button to a control panel, which is the only push-button control in the module and therefore the only route to a conventional start or stop control rather than a slider. | Interaction | Public installed API but Qt-only, and `WITH_QT` is declared OFF by default [CMakeLists.txt:299-301]. The runtime backend table's Qt entry is additionally compiled out behind a `#if 0  // TODO` guard [modules/highgui/src/registry.impl.hpp:51], and the code records no reason for that. A button-based control surface therefore cannot be assumed present. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `getWindowProperty`, `setWindowTitle`, `resizeWindow` [modules/highgui/include/opencv2/highgui.hpp:403,390,356,362] | Reads a window property, sets a window title, and resizes a window in a width-and-height overload and a size overload. These are the window-management calls a review surface would use to label and size its panes. | Preview and review | Public installed API, declared unconditionally in the installed header [modules/highgui/include/opencv2/highgui.hpp:403], and conditional on the active backend: the framebuffer backend implements neither property accessor nor the title setter, logging that each is not supported [modules/highgui/src/window_framebuffer.cpp:268,274,319]. Gated per the display options in section 5. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| Runtime user-interface backend table — `getBuiltinBackendsInfo` [modules/highgui/src/registry.impl.hpp:27-66] | The complete membership of runtime user-interface backends, and therefore the definitive list of what could be active. It declares six identifiers: `GTK`, `GTK3`, `GTK2`, `FB`, `QT` and `WIN32`. Neither Wayland nor Cocoa appears in it, so neither can be the value a runtime backend probe reports through this table. | Backend discovery | Private, non-installed: this table lives in a module-internal header [modules/highgui/src/registry.impl.hpp:27-66] and is not application-facing, though its entries are the values the public probe reports. Each entry is compiled in only under its own build guard, and the `QT` entry is compiled out behind a `#if 0  // TODO` guard [modules/highgui/src/registry.impl.hpp:51], with no reason recorded in the code. No configure step was run against this checkout, so which entries this table would hold is unknown here. | no |
| `GTK`, `GTK3`, `GTK2` runtime backends [modules/highgui/src/registry.impl.hpp:33,35,37] | The GTK-family runtime backends, registered statically where GTK is available and as dynamically loaded entries otherwise [modules/highgui/src/registry.impl.hpp:42,43,44]. This is the display backend a Linux deployment would ordinarily use, and the only one on which the event-thread facility above has any effect. | Preview and review | Registered under `HAVE_GTK` or, failing that, as plugin entries when plugins are enabled [modules/highgui/src/registry.impl.hpp:33,42]. Gated by `WITH_GTK`, declared ON, visible only on non-Apple Unix, and verified against a detected GTK [CMakeLists.txt:225-227], with `WITH_GTK_2_X` declared OFF selecting the version-2 path [CMakeLists.txt:228-230]. Requires the GTK toolkit at build and run time. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `FB` framebuffer runtime backend [modules/highgui/src/registry.impl.hpp:48] | A display backend that draws into a mapped framebuffer surface rather than into a window managed by a toolkit. Its construction selects one of three modes — a physical framebuffer device, an emulation mode, and a mode backed by a virtual framebuffer file [modules/highgui/src/window_framebuffer.cpp:572-602]. In the physical and virtual modes it reads the mapped surface once into a four-channel image at window construction [modules/highgui/src/window_framebuffer.cpp:626-633] so that the contents present before OpenCV drew can be restored afterwards. That read is the only place in the in-scope modules where display contents are read, it happens once per window rather than in a loop, and it is not exposed through any capture API as a source of frames. | Preview and review | Registered under `HAVE_FRAMEBUFFER` [modules/highgui/src/registry.impl.hpp:48]. Both gates are declared OFF: `WITH_FRAMEBUFFER` [CMakeLists.txt:231-232] and `WITH_FRAMEBUFFER_XVFB` [CMakeLists.txt:233-234], so neither path is present in a default build. It is also the backend that makes interaction verdicts conditional, implementing none of pointer callbacks, trackbars, window properties or window titles and logging each as not supported [modules/highgui/src/window_framebuffer.cpp:268,274,319,322-324,327-333,339]. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `QT` runtime backend [modules/highgui/src/registry.impl.hpp:53,55] | The Qt runtime backend, which is the only backend under which the button control above exists. | Preview and review | Declared but not reachable as built: both the static and the plugin registration sit inside a region compiled out behind a `#if 0  // TODO` guard [modules/highgui/src/registry.impl.hpp:51], which is evidence of work left incomplete and records no reason in the code. Gated additionally by `WITH_QT`, declared OFF [CMakeLists.txt:299-301]. Requires the Qt toolkit. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `WIN32` runtime backend [modules/highgui/src/registry.impl.hpp:61,63] | The Windows runtime backend, registered only within a Windows-only region of the table [modules/highgui/src/registry.impl.hpp:61]. This is the identifier a runtime probe reports on Windows; the corresponding build option carries a different name. | Preview and review | Registered under `HAVE_WIN32UI`, or as a plugin entry when plugins are enabled [modules/highgui/src/registry.impl.hpp:61,63]. Gated by `WITH_WIN32UI`, declared ON but visible only on Windows and not on the Windows Runtime target [CMakeLists.txt:302-304]. Requires the platform user-interface library. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| Internal user-interface backend interface — `getCurrentUIBackend`, `setUIBackend` [modules/highgui/src/backend.hpp:112,113,114] | The module-internal interface through which a backend is selected and reached, with a shared-pointer setter and a setter taking a backend name. | Backend discovery | Private, non-installed: declared in a module-internal header [modules/highgui/src/backend.hpp:112,113,114] and not part of the application-facing API, so it must not be mistaken for a supported way to select a display backend from an application. No build option gates the declaration. No configure step was run against this checkout, so which backend it would resolve is unknown here. | no |
| System-wide or background keyboard and pointer input hook | Delivery of keyboard and pointer events that occur anywhere on the desktop, rather than only over a window this process owns. This is what a notetaking timeline needs in order to correlate a user action with a visual change. Not Found. | Interaction | Not Found. Established by enumeration of the module's public event surface: input is window-scoped by construction, with the pointer-movement event documented as movement over the window [modules/highgui/include/opencv2/highgui.hpp:129], the event-fetching functions documented as working only when a window exists and is active [modules/highgui/include/opencv2/highgui.hpp:282-287], and pointer callbacks registered per window [modules/highgui/include/opencv2/highgui.hpp:427]. No entry point in the installed header takes a desktop or system scope, and no build option gates a capability that is not declared. | yes |
| Multi-panel layout, docking, menus | Arrangement of several independently sized panes within one window, with dockable regions and a menu bar, which is what a review-and-edit surface for captured frames and a timeline would require. Not Found. | Interaction | Not Found. Established by enumeration of the installed header's window-management surface, which offers window creation, destruction, resizing, titling and property access on whole named windows [modules/highgui/include/opencv2/highgui.hpp:239,247,356,362,390,403] and no container, layout, dock or menu entry point at all. The module's own account of itself frames it as a facility for trying functionality out quickly and visualising results [modules/highgui/include/opencv2/highgui.hpp:55-66], which is consistent with the absence. No build option gates a capability that is not declared. | yes |
| Text entry control | A control accepting typed text, which a notetaking surface needs for a title, a tag or an annotation body. Not Found. The keyboard surface is a key-code return from the event-pump functions [modules/highgui/include/opencv2/highgui.hpp:271,291,305], which reports keys and holds no editable text state. | Interaction | Not Found. Established by enumeration of the installed header's complete control surface, which is trackbars [modules/highgui/include/opencv2/highgui.hpp:517,532,545,558,571] and a Qt-only button [modules/highgui/include/opencv2/highgui.hpp:808-810]; no text-input, field or editor entry point is declared. No build option gates a capability that is not declared. | yes |


# 4. Inference-Relevant APIs

| API or module name | Purpose | Pipeline stage | Maturity / status in this build | External dependency required |
| --- | --- | --- | --- | --- |
| `cv::dnn::Net`, with `setInput` and `forward` [modules/dnn/include/opencv2/dnn/dnn.hpp:566,834,725] | The network object and the two calls that constitute one inference pass: bind an input blob, then run the graph and return the output. This is the base capability on which every classification, detection and text model below is built. | Inference | Public installed API, declared unconditionally in the installed header [modules/dnn/include/opencv2/dnn/dnn.hpp:566]. It is graph machinery only and holds no model: a network must be loaded through one of the readers below from a file supplied from outside this repository. Which computation backend and target actually execute the pass is a run-time selection, not a property of this declaration. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `readNetFromTensorflow`, `readNetFromTFLite`, `readNet`, `readNetFromONNX` [modules/dnn/include/opencv2/dnn/dnn.hpp:1121,1161,1201,1261] | The four network readers, each declared in both a file-path and an in-memory-buffer overload [modules/dnn/include/opencv2/dnn/dnn.hpp:1134,1169,1217,1281]. They are the only route by which a model enters the library, and the framework-agnostic reader is what lets an application accept whichever format an obtained model happens to use. | Inference | Public installed API, declared unconditionally in the installed header [modules/dnn/include/opencv2/dnn/dnn.hpp:1121]. Each reader requires a model file, and for some formats a companion configuration file, supplied from outside this repository; the readers parse assets, they do not provide them. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `blobFromImage`, `blobFromImageWithParams` [modules/dnn/include/opencv2/dnn/dnn.hpp:1308,1418] | Converts an image into the batched, scaled, mean-subtracted tensor a network expects, the parameterised form taking a parameter object and also being declared in an output-argument overload [modules/dnn/include/opencv2/dnn/dnn.hpp:1421]. This is the bridge from a captured frame to an inference input. | Inference preprocessing | Public installed API, declared unconditionally in the installed header [modules/dnn/include/opencv2/dnn/dnn.hpp:1308]. It is pure preprocessing and requires no model or asset of its own, which makes it the one row in this section with no external prerequisite. No build option gates the declaration. No configure step was run against this checkout, so the configured build was not verified here. | no |
| `ClassificationModel` [modules/dnn/include/opencv2/dnn/dnn.hpp:1656] | A high-level wrapper returning a class identifier and score for an image. For a screen workload this is the route to labelling a captured frame or a cropped region as being of some kind, once a taxonomy and a model for it exist. | Inference | Public installed API, declared unconditionally in the installed header [modules/dnn/include/opencv2/dnn/dnn.hpp:1656]. It is a wrapper over the network object above and supplies no model or label set; both come from outside this repository. Its default constructor carries the same external-deprecation marker used elsewhere in this header for constructors awaiting a bindings fix [modules/dnn/include/opencv2/dnn/dnn.hpp:1659], while the class itself and its other constructors are not deprecated. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `DetectionModel` [modules/dnn/include/opencv2/dnn/dnn.hpp:1772] | A high-level wrapper returning class identifiers, confidences and bounding boxes for an image, which is the general object-detection shape as distinct from the text-specific detectors below. | Inference | Public installed API, declared unconditionally in the installed header [modules/dnn/include/opencv2/dnn/dnn.hpp:1772]. A wrapper only: it supplies no detector model. Its default constructor carries the external-deprecation marker used elsewhere in this header for constructors awaiting a bindings fix [modules/dnn/include/opencv2/dnn/dnn.hpp:1789]; the class and its other constructors are not deprecated. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `TextDetectionModel`, with `detect` [modules/dnn/include/opencv2/dnn/dnn.hpp:1910,1937] | The base text-detection wrapper. Its detection call yields quadrangles with a parallel array of detection confidences, and a second overload returns the geometry alone [modules/dnn/include/opencv2/dnn/dnn.hpp:1945]. This is where a text region on a captured screen frame is located, prior to any recognition. | Text extraction | Public installed API, declared unconditionally in the installed header [modules/dnn/include/opencv2/dnn/dnn.hpp:1910]. Abstract with respect to assets: a detector network must be supplied. Its default constructor is marked as external-deprecated and noted as moving to protected once bindings are fixed [modules/dnn/include/opencv2/dnn/dnn.hpp:1913]; the class and its non-default constructors are not deprecated. The confidence it reports is a detector confidence about a region, not a confidence about any recognised string. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `TextDetectionModel_EAST`, with `setConfidenceThreshold` and `setNMSThreshold` [modules/dnn/include/opencv2/dnn/dnn.hpp:1983,2010,2023] | The first of the two concrete text detectors, with its two tuning knobs: a confidence floor for accepting a region and a suppression threshold for merging overlapping ones. | Text extraction | Public installed API, declared unconditionally in the installed header [modules/dnn/include/opencv2/dnn/dnn.hpp:1983]. Constructible either from a caller-supplied network [modules/dnn/include/opencv2/dnn/dnn.hpp:1993] or from model and configuration paths, so the wrapper exists while the matching detector weights do not. Its default constructor is marked as external-deprecated and noted as moving to protected once bindings are fixed [modules/dnn/include/opencv2/dnn/dnn.hpp:1986]; the class and its non-default constructors are not deprecated. No numeric accuracy figure is committed anywhere in this repository. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `TextDetectionModel_DB`, with `setBinaryThreshold`, `setPolygonThreshold`, `setUnclipRatio` and `setMaxCandidates` [modules/dnn/include/opencv2/dnn/dnn.hpp:2044,2066,2069,2072,2075] | The second concrete text detector, with four tuning knobs governing binarisation, polygon acceptance, region expansion and the candidate cap. | Text extraction | Public installed API, declared unconditionally in the installed header [modules/dnn/include/opencv2/dnn/dnn.hpp:2044]. Constructible either from a caller-supplied network [modules/dnn/include/opencv2/dnn/dnn.hpp:2054] or from model and configuration paths; no matching weights are provided here. Its default constructor is marked as external-deprecated and noted as moving to protected once bindings are fixed [modules/dnn/include/opencv2/dnn/dnn.hpp:2047]; the class and its non-default constructors are not deprecated. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `TextRecognitionModel`, with `setVocabulary`, `setDecodeType` and `setDecodeOptsCTCPrefixBeamSearch` [modules/dnn/include/opencv2/dnn/dnn.hpp:1827,1880,1857,1873] | The text-recognition wrapper: it turns a detected region into a string. Recognition is performed by a call returning a single string and by an overload taking regions of interest and returning one string each [modules/dnn/include/opencv2/dnn/dnn.hpp:1895,1904]. The header states that for this class CRNN-CTC is supported [modules/dnn/include/opencv2/dnn/dnn.hpp:1818-1825], and the decode setters select between greedy and prefix-beam-search CTC decoding. | Text extraction | Public installed API, declared unconditionally in the installed header [modules/dnn/include/opencv2/dnn/dnn.hpp:1827]. Conditional on three caller-supplied things, not one: a network, given either directly [modules/dnn/include/opencv2/dnn/dnn.hpp:1838] or as model and configuration paths; a decode type; and a character vocabulary set through `setVocabulary` [modules/dnn/include/opencv2/dnn/dnn.hpp:1880], without which recognition has no output alphabet. The recognition calls return strings and no per-string confidence, so a recognition confidence is not available from this API. Its default constructor is marked as external-deprecated and noted as moving to protected once bindings are fixed [modules/dnn/include/opencv2/dnn/dnn.hpp:1830]; the class and its non-default constructors are not deprecated. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| Bundled text-recognition model weights, model configuration and character vocabulary assets | The three artefacts the text wrappers above require before any of them can run: detector weights matching one of the two concrete detectors, recogniser weights matching the CRNN-CTC form the header documents, and a character vocabulary. Not Found within the in-scope source domain. This absence is scoped to the in-scope modules and is not a claim about the repository as a whole. | Text extraction | Not Found. Established by a complete enumeration of the four in-scope module trees, all 1086 files of them, for model, configuration and vocabulary artefacts, which returns none, together with the wrappers' own contracts, each of which requires the caller to supply a network or model and configuration paths and, for recognition, a vocabulary [modules/dnn/include/opencv2/dnn/dnn.hpp:1838,1880,1993,2054]. There is no build option for a text-recognition engine anywhere in the build configuration and no separate text module in the tree. No build option gates an asset that is not present. | yes |
| Action taxonomy for screen interactions | A defined set of user-action classes — the vocabulary of what counts as an action, such as a click, a text entry or a navigation — against which observed screen change and input events could be labelled. Not Found. Neither a taxonomy, a model trained against one, nor labelled screen-recording data exists here, so learned visual action classification has no starting point in this repository and its prerequisites must be supplied from outside it. | Inference | Not Found. Established by enumeration of the high-level model wrappers declared in the installed header, which are a classification wrapper, a general detection wrapper and the text detection and recognition wrappers [modules/dnn/include/opencv2/dnn/dnn.hpp:1656,1772,1910,1827] and nothing action-specific, and by the same complete enumeration of the four in-scope module trees, which finds no label set or class list of any kind. No build option gates a capability that is not declared. | yes |


# 5. Build/Plugin Flags Relevant to Deployment Target

| API or module name | Purpose | Pipeline stage | Maturity / status in this build | External dependency required |
| --- | --- | --- | --- | --- |
| `WITH_GSTREAMER` | Gates the GStreamer backend, and therefore gates the stronger of the two routes by which externally acquired screen frames can reach the capture API. | Frame acquisition | Declared default ON [CMakeLists.txt:222]. Gated further by visibility on non-Android, non-iOS, non-visionOS and non-Windows-Runtime targets and by a verification clause requiring a detected GStreamer newer than version 0.99 [CMakeLists.txt:223-224], so requesting the option does not by itself make the backend available. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `WITH_FFMPEG` | Gates the FFmpeg backend, and therefore gates the weaker, environment-mediated ingestion route, the libavdevice-conditional device opening, and the four FFmpeg-only capture properties in section 1. | Frame acquisition | Declared default is the conditional expression `(NOT ANDROID)` rather than a plain ON [CMakeLists.txt:219]. Gated further by visibility on non-iOS, non-visionOS and non-Windows-Runtime targets and by a verification clause requiring a detected FFmpeg [CMakeLists.txt:220-221]. Device opening additionally requires libavdevice in the detected FFmpeg [modules/videoio/src/cap_ffmpeg_impl.hpp:1213]. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `WITH_V4L` | Gates Video4Linux capture on Linux, which is also the one backend on which the frame-readiness notification API of section 1 is implemented. | Frame acquisition | Declared default ON [CMakeLists.txt:320]. Gated further by visibility on non-Apple, non-Android Unix and by a verification clause requiring one of the detected Video4Linux or video-input capabilities [CMakeLists.txt:321-322]. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `WITH_DSHOW` | Gates the DirectShow capture backend, one of the two Windows camera paths a Windows deployment would choose between. | Frame acquisition | Declared default ON [CMakeLists.txt:323]. Gated further by visibility on Windows only, excluding ARM and the Windows Runtime target, and by a verification clause requiring detected DirectShow [CMakeLists.txt:324-325]. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `WITH_MSMF` | Gates the Media Foundation capture backend, the other Windows camera path. Relevant additionally because this backend is where the Direct3D references in the tree live, and those are hardware decode plumbing rather than any form of display duplication. | Frame acquisition | Declared default is the conditional expression `NOT MINGW` rather than a plain ON [CMakeLists.txt:326]. Gated further by visibility on Windows only and by a verification clause requiring detected Media Foundation [CMakeLists.txt:327-328]. The Direct3D device-manager plumbing in this backend serves hardware-accelerated decode [modules/videoio/src/cap_msmf.cpp:1002-1013], as does that in the FFmpeg hardware path [modules/videoio/src/cap_ffmpeg_hw.hpp:232-243]; neither is a screen-duplication interface. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `WITH_AVFOUNDATION` | Gates the AVFoundation media backend on Apple platforms, which is that platform family's capture path. | Frame acquisition | Declared default ON [CMakeLists.txt:171]. Gated further by visibility on Apple platforms only and by a verification clause requiring detected AVFoundation [CMakeLists.txt:172-173], so it is inert on the other targets. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `WITH_GTK` | Gates the GTK-family display backends, which are the display path a Linux deployment would ordinarily use and the only one under which the event-thread facility of section 3 has any effect. | Preview and review | Declared default ON [CMakeLists.txt:225]. Gated further by visibility on non-Apple, non-Android Unix and by a verification clause requiring a detected GTK [CMakeLists.txt:226-227]. Requires the GTK toolkit at build and run time. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `WITH_GTK_2_X` | Selects the version-2 GTK path rather than version 3, which determines which of the GTK-family runtime identifiers of section 3 a deployment would report. | Preview and review | Declared default OFF [CMakeLists.txt:228]. Gated further by visibility on non-Apple, non-Android Unix and by a verification clause requiring a detected GTK that is not GTK 3 [CMakeLists.txt:229-230]. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `WITH_QT` | Gates the Qt display backend, which is the only backend under which the button control of section 3 exists. | Interaction | Declared default OFF [CMakeLists.txt:299]. Gated further by visibility on non-Android, non-iOS, non-visionOS and non-Windows-Runtime targets and by a verification clause requiring a detected Qt [CMakeLists.txt:300-301]. Independently of this option, the runtime backend table's Qt entry is compiled out behind a `#if 0  // TODO` guard [modules/highgui/src/registry.impl.hpp:51], and the code records no reason for that; enabling the option therefore does not on its own establish that the runtime backend becomes available. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `WITH_WIN32UI` | Gates the Windows display backend. Its name differs from the runtime identifier the probe of section 3 reports, which is `WIN32`; the two must not be conflated. | Preview and review | Declared default ON [CMakeLists.txt:302]. Gated further by visibility on Windows only, excluding the Windows Runtime target, and by a verification clause requiring the detected Windows user-interface capability [CMakeLists.txt:303-304]. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `WITH_WAYLAND` | Gates the Wayland display backend. Relevant to a deployment target as one of the display options a Linux build chooses among, and notable for carrying both the heaviest detection requirement and the only experimental marking of any display option inventoried here. | Preview and review | Declared default OFF [CMakeLists.txt:235], gated further by visibility on non-Apple, non-Android Unix and by a verification clause requiring a detected Wayland [CMakeLists.txt:236-237]. The build summary prints this capability as `(Experimental) YES` when it is enabled [CMakeLists.txt:1396] — the project's own maturity assessment, stated as status and giving no reason. Detection requires four separate components, wayland-client, wayland-cursor, xkbcommon and wayland-protocols at version 1.13 or newer, and additionally requires the wayland-scanner host program, which is located as a required program [modules/highgui/cmake/detect_wayland.cmake:12-32]; the capability is set only when all four are found. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `WITH_FRAMEBUFFER` | Gates the framebuffer display backend, whose physical mode is the one place in the in-scope modules that reads a display surface, once per window and for restoration only, as recorded in section 3. | Preview and review | Declared default OFF [CMakeLists.txt:231], so this path is absent from a default build. Gated further by visibility on non-Apple, non-Android Unix, and notably it declares no dependency-verification clause at all [CMakeLists.txt:232], unlike the toolkit options above; what it needs is a framebuffer device provided by the host rather than a detectable library. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `WITH_FRAMEBUFFER_XVFB` | Gates the virtual-framebuffer mode of the same backend, which reads its geometry and pixel layout from a virtual framebuffer file rather than from a device. | Preview and review | Declared default OFF [CMakeLists.txt:233], so this path too is absent from a default build. Gated further by visibility on non-Apple, non-Android Unix, and it declares no dependency-verification clause [CMakeLists.txt:234]. The file it maps is produced by a virtual display server outside OpenCV, and nothing in the in-scope modules establishes how or how often that file is refreshed. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `WITH_OPENGL` | Gates OpenGL support in the display layer, which bears on a deployment target because it changes what a display backend can present and which window flags are meaningful. | Preview and review | Declared default OFF [CMakeLists.txt:275]. Gated further by visibility on non-Android, non-Windows-Runtime targets and by a verification clause requiring detected OpenGL [CMakeLists.txt:276-277]. No configure step was run against this checkout, so configured availability is unknown here. | yes |
| `VIDEOIO_PLUGIN_LIST`, `VIDEOIO_ENABLE_PLUGINS` [modules/videoio/CMakeLists.txt:6,7] | The two switches deciding whether capture backends are built into the library or shipped as separately loadable plugins, and which ones. This is the deployment decision that determines whether a media backend can be added to an installed application without rebuilding it. | Source discovery | Declared defaults: the list is an empty cache string [modules/videoio/CMakeLists.txt:6] and the enable switch takes a computed default that is ON except on Emscripten, iOS, visionOS and the Windows Runtime target [modules/videoio/CMakeLists.txt:1-3,7]. The enable switch dominates the list: when plugins are disabled a non-empty list is discarded with a warning [modules/videoio/CMakeLists.txt:12-15]. Both are marked advanced [modules/videoio/CMakeLists.txt:8]. No configure step was run against this checkout, so configured availability is unknown here. | no |
| `HIGHGUI_PLUGIN_LIST`, `HIGHGUI_ENABLE_PLUGINS` [modules/highgui/cmake/init.cmake:6,7] | The same decision for display backends: whether a user-interface backend is built in or loadable, and which ones. | Preview and review | Declared defaults: the list is an empty cache string [modules/highgui/cmake/init.cmake:6] and the enable switch takes a computed default that is ON except on Emscripten and iOS [modules/highgui/cmake/init.cmake:2-4,7]; both are marked advanced [modules/highgui/cmake/init.cmake:8]. The enable switch both discards a list supplied while plugins are disabled, with a warning [modules/highgui/cmake/init.cmake:12-15], and gates whether the module is compiled with plugin support at all [modules/highgui/CMakeLists.txt:272]. No configure step was run against this checkout, so configured availability is unknown here. | no |
| Capture plugin binary interface — `opencv_videoio_capture_plugin_init_v1` [modules/videoio/src/plugin_capture_api.hpp:16,22] | The interface a loadable capture backend must implement, declaring a capture interface version of 2 and a binary interface version of 1 [modules/videoio/src/plugin_capture_api.hpp:16,22]. Frame delivery across it is strictly pull-based: an open, a grab and a retrieve with a copy-out callback [modules/videoio/src/plugin_capture_api.hpp:56,92,103], extended by an open-with-parameters entry [modules/videoio/src/plugin_capture_api.hpp:118] and a stream-open entry [modules/videoio/src/plugin_capture_api.hpp:139]. There is no push or notification entry, so a plugin cannot signal that a frame has become available. | Frame acquisition | Private, non-installed: declared in a module-internal header [modules/videoio/src/plugin_capture_api.hpp:41] and not part of the application-facing API. It does not permit a new backend to be introduced from outside the library: discovery is organised around identifiers the registry already knows, by a fixed library-name pattern and a per-backend override [modules/videoio/src/backend_plugin.cpp:302,310,331,332], and after a plugin returns its function table the loader compares the backend identifier that table declares against the one the plugin was loaded for and abandons the plugin on a mismatch [modules/videoio/src/backend_plugin.cpp:400-425], in addition to a version-compatibility check [modules/videoio/src/backend_plugin.cpp:146]. Gated by the videoio plugin switches above. No configure step was run against this checkout, so configured availability is unknown here. | no |
| User-interface plugin binary interface — `opencv_ui_plugin_init_v0` [modules/highgui/src/plugin_api.hpp:63] | The interface a loadable display backend must implement, exposing a single instance-getter entry point [modules/highgui/src/plugin_api.hpp:44]. | Preview and review | Private, non-installed, and preview-grade by its own declaration: both the interface version and the binary interface version are 0 and each is annotated `// preview` in the source [modules/highgui/src/plugin_api.hpp:18,24]. Its discovery path is also incomplete: the candidate search reads a plugin path variable belonging to another module, with an in-source `// TODO OPENCV_PLUGIN_PATH` marker directly above the line that reads it [modules/highgui/src/plugin_wrapper.impl.hpp:174,182,183], and no user-interface-specific plugin path variable exists anywhere in the modules tree; only the library-name pattern and the per-backend override are complete [modules/highgui/src/plugin_wrapper.impl.hpp:204,205]. The code records no reason for the incomplete state. Gated by the highgui plugin switches above. No configure step was run against this checkout, so configured availability is unknown here. | no |
| Screen-capture build option, and text-recognition engine build option | A build option that would enable acquisition of display, desktop, window or monitor content, and a build option that would enable an optical character recognition engine. Not Found: neither exists, so neither capability can be turned on at configure time and both must be met from outside OpenCV. | Frame acquisition | Not Found. Established by a complete enumeration rather than a keyword search: the assessed capture and display options are exactly those declared in the option block inventoried above [CMakeLists.txt:219-235,275,299-302,320-329], and collecting every option name declared across every build configuration file in the repository yields 186 distinct names, of which not one names a screen, display, desktop or monitor source and not one names a text-recognition engine. Camera and machine-vision device options declared in the same block are deliberately excluded from this section, since none gates an assessed route or one of the three target platforms. | yes |
